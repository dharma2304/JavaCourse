package Collections.SetInterface;

public class DescriptionOfSetInterface {
    /*
      Set: Set is an interface which extends the collection interface.It's an unordered-collection of objects and no duplicate objects are
           allowed. The set allows atmost one null element.
     */
}