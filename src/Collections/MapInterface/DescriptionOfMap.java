package Collections.MapInterface;

public class DescriptionOfMap {
    /*
      Map: It's a separate interface which dont extends the collection interface. Its not subtype of collection interface.
           Maps stored in a key-value pair format where the keys are unique.Maps dont allow duplicate keys and each key has atmost one value.
     */
}